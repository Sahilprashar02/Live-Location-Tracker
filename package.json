{
  "name": "live-location-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time authenticated live location tracking with Socket.IO, Kafka, and Leaflet.",
  "scripts": {
    "dev": "concurrently -k \"PORT=4010 CLIENT_ORIGIN=http://localhost:5174 npm run dev --workspace server\" \"VITE_API_URL=http://localhost:4010 npm run dev --workspace client -- --port 5174 --strictPort\"",
    "dev:default": "concurrently -k \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:fallback": "npm run dev",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "db-processor": "npm run db-processor --workspace server",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
